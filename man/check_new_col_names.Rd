% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{check_new_col_names}
\alias{check_new_col_names}
\title{Check that a data frame does not already have given column names}
\usage{
check_new_col_names(
  data,
  cols,
  ...,
  arg = caller_arg(data),
  call = caller_env()
)
}
\arguments{
\item{data}{A data frame to check.}

\item{cols}{Character vector of column names that must not already exist
in \code{data}.}

\item{arg, call}{Passed to \code{\link[cli:cli_abort]{cli::cli_abort()}} for error attribution.}
}
\description{
\code{check_new_col_names()} errors if \code{data} already has any column named in
\code{cols}, for use before adding new columns that are not expected to already
be present (e.g. to avoid silently overwriting an existing column or
creating a confusing \code{.x}/\code{.y} suffix after a join).
}
\keyword{internal}
