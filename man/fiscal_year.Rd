% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fiscal_year.R
\name{fiscal_year}
\alias{fiscal_year}
\title{Convert an object to a fiscal year string or date}
\usage{
fiscal_year(
  x,
  type = c("year", "year_prefix", "year_prefix_abb", "date_first", "date_last"),
  before = "FY",
  ...
)
}
\arguments{
\item{x}{a vector of \link{POSIXt}, numeric or character objects}

\item{type}{One of "year", "year_prefix", "year_prefix_abb", "date_first", or
"date_last". Defaults to "year".}

\item{before}{Text to use as prefix if type is "year_prefix"
or "year_prefix_abb". Ignored for other types. Defaults to "FY".}

\item{...}{
  Arguments passed on to \code{\link[lubridate:as_date]{lubridate::as_date}}
  \describe{
    \item{\code{tz}}{a time zone name (default: time zone of the POSIXt object \code{x}). See
\code{\link[=OlsonNames]{OlsonNames()}}.}
    \item{\code{origin}}{a Date object, or something which can be coerced by \code{as.Date(origin, ...)} to such an object (default: the Unix epoch of "1970-01-01"). Note that in
this instance, \code{x} is assumed to reflect the number of days since \code{origin} at
\code{"UTC"}.}
    \item{\code{format}}{format argument for character methods. When supplied parsing is
performed by \code{parse_date_time(x, orders = formats, exact = TRUE)}. Thus, multiple
formats are supported and are tried in turn.}
  }}
}
\value{
An integer (if type = "year"), character, or date (if type is
"date_first" or "date_last").
}
\description{
\code{\link[=fiscal_year]{fiscal_year()}} uses \code{\link[lubridate:as_date]{lubridate::as_date()}} to return a fiscal year value
based on any input vector coercible to dates.
}
\examples{
fiscal_year("2021-01-01")

fiscal_year("2021-07-01")

fiscal_year("2021-01-01", "year_prefix")

fiscal_year("2021-01-01", "year_prefix_abb")

fiscal_year("2021-01-01", "date_first")

fiscal_year("2021-01-01", "date_last")

}
