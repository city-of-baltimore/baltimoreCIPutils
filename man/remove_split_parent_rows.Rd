% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports.R
\name{remove_split_parent_rows}
\alias{remove_split_parent_rows}
\title{Remove parent rows from split budget requests in program data}
\usage{
remove_split_parent_rows(
  program_data,
  key_cols = c("ProjectID", "CostCenterID", "FundID", "GrantID", "RevenueCategoryID",
    "Account", "RequestType", "ChangeType", "FiscalYear", "ProgramVersion", "AgencyID"),
  call = caller_env()
)
}
\arguments{
\item{program_data}{Program data with a logical \code{SplitChildRow} column and
the columns named in \code{key_cols}.}

\item{key_cols}{Character vector of column names that, together, identify
a single split budget request: a parent row and its \code{SplitChildRow == TRUE} children share every one of these values, differing only in
\code{Phase} and the \code{Year1}..\code{Year6} amounts.}

\item{call}{Passed to \code{\link[cli:cli_abort]{cli::cli_abort()}} for error attribution.}
}
\value{
\code{program_data} with parent rows of split budget requests removed.
}
\description{
\code{program_data} can include, for a single budget request, both a "parent"
row that aggregates the request across phases and one "child" row per
phase (flagged \code{SplitChildRow == TRUE}); the parent's amount in every
\code{Year1}..\code{Year6} column is the sum of its children's amounts in that same
column. This function removes each such parent row -- identified as a
\code{SplitChildRow == FALSE} row that shares every value in \code{key_cols} with at
least one \code{SplitChildRow == TRUE} row -- so downstream totals aren't
double-counted.
}
\details{
Unlike matching on \code{Phase == "<multiple>"} (a placeholder set upstream
when a split's children span more than one phase), this identifies parent
rows directly from \code{SplitChildRow}, so it isn't fooled by a split whose
children happen to all share a single phase.

\code{key_cols} is not guaranteed to be a unique row identity: two unrelated,
independently entered rows can legitimately share every \code{key_cols} value
(e.g. the same request logged for two different phases without being a
split). That's harmless on its own, but if such a row ever coincided with
a real split under the same key, there would be no way to tell which
non-child row is the true parent to drop. Rather than guess, this function
aborts with \code{\link[cli:cli_abort]{cli::cli_abort()}} when a \code{key_cols} group has both more than
one \code{SplitChildRow == FALSE} row and at least one \code{SplitChildRow == TRUE}
row, so the duplicate can be resolved upstream instead.
}
\seealso{
\code{\link[=add_group_key]{add_group_key()}}
}
\keyword{internal}
