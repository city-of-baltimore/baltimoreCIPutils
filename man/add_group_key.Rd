% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports.R
\name{add_group_key}
\alias{add_group_key}
\title{Add a row-wise identity key column}
\usage{
add_group_key(data, key_cols, key_name = ".group_key")
}
\arguments{
\item{data}{A data frame.}

\item{key_cols}{Character vector of column names in \code{data} to concatenate
into the identity key.}

\item{key_name}{Name to use for the new key column.}
}
\value{
\code{data} with an additional column named \code{key_name} containing the
concatenated key for each row.
}
\description{
Concatenates \code{key_cols} into a single text column, one value per row, so
rows that share the same combination of \code{key_cols} values can be detected
(see \code{\link[=remove_split_parent_rows]{remove_split_parent_rows()}}). \code{key_cols} are cast to character in
temporary columns before concatenating, because \code{dplyr::c_across()}
combines columns with \code{vctrs::vec_c()}, which -- unlike \code{paste()} -- errors
when mixing character and numeric columns without an explicit cast; using
temporary columns also keeps \code{key_cols}'s original types unchanged in the
returned data.
}
\keyword{internal}
