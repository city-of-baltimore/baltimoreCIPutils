% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports.R
\name{fmt_active_estimate_data}
\alias{fmt_active_estimate_data}
\title{Filter estimate data to active estimates and subset n per project}
\usage{
fmt_active_estimate_data(
  estimate_data,
  n = 1,
  active_submission_status = c("Submitted", "Draft"),
  call = caller_env()
)
}
\arguments{
\item{estimate_data}{Cost estimate data with columns \code{ProjectID},
\code{SubmissionStatus}, \code{PlanningCost}, \code{DesignCost}, \code{ConstructionCost},
\code{ContingencyCost}, and \code{ManagementCost}.}

\item{n}{Number of active estimates to keep per project.}

\item{active_submission_status}{One or more SubmissionStatus values
("Submitted", "Draft", "New", or "Archived") used to subset \code{estimate_data}
to active estimate/schedule information. Defaults to \code{c("Submitted", "Draft")}.}

\item{call}{Passed to \code{\link[rlang:arg_match]{rlang::arg_match()}} for error attribution.}
}
\value{
\code{estimate_data} filtered to active (non-archived, non-new)
estimates, with a \code{TotalCost} column added, subset to the first \code{n}
estimates per \code{ProjectID}.
}
\description{
Drops estimates with \code{SubmissionStatus} of \code{"Archived"} or \code{"New"},
computes \code{TotalCost} as the sum of the planning, design, construction,
contingency, and management cost columns, and keeps the first \code{n}
remaining estimates per project, preferring \code{"Submitted"} over \code{"Draft"}
status. Warns (via \code{\link[cli:cli_inform]{cli::cli_inform()}}) if any project has more than one
remaining draft or submitted estimate.
}
\keyword{internal}
