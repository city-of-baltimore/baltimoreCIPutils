% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{filter_program_data}
\alias{filter_program_data}
\title{Filter program data by program version}
\usage{
filter_program_data(program_data, program_version, multiple = FALSE, ...)
}
\arguments{
\item{program_data}{A data frame of program data with a \code{ProgramVersion}
column, such as program data from the Six-Year CIP sheet.}

\item{program_version}{A string (or, if \code{multiple = TRUE}, a character
vector) of program versions to keep. Must match values in the
\code{ProgramVersion} column of \code{program_data}.}

\item{multiple}{If \code{TRUE}, allow \code{program_version} to match more than one
version. Passed to \code{\link[rlang:arg_match]{rlang::arg_match()}}. Defaults to \code{FALSE}.}

\item{...}{Additional filter expressions passed to \code{\link[dplyr:filter]{dplyr::filter()}} and
combined with the \code{ProgramVersion} filter.}
}
\value{
A filtered data frame with the same columns as \code{program_data}.
}
\description{
\code{filter_program_data()} is a convenience function for Capital Improvement
Program (CIP) reporting that filters program data to one or more values of
the \code{ProgramVersion} column. \code{program_version} is matched against the values
present in \code{program_data}, so a misspelled or missing version results in an
informative error rather than an empty data frame.
}
\examples{
program_data <- data.frame(
  ProgramVersion = c("Planning", "Planning", "Adopted"),
  ProjectID = c("100-001", "100-002", "100-001"),
  Amount = c(100, 250, 150)
)

filter_program_data(program_data, "Planning")

filter_program_data(
  program_data,
  c("Planning", "Adopted"),
  multiple = TRUE,
  Amount > 100
)
}
